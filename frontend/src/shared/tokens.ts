// Design tokens shared between the landing page and the (post-MVP) Mini App.
// Keep this minimal — Tailwind handles most styling.

export const tokens = {
  colors: {
    accent: '#f97316', // warm orange per CLAUDE.md §13
  },
} as const
